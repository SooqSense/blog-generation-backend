name: Deploy Django to Cloud Run (production)

on:
  push:
    branches: [production]
  workflow_dispatch:

env:
  PROJECT_ID: artilence
  REGION: asia-south2
  REPOSITORY: sooqsense-bloggenerator
  SERVICE_NAME: sooqsense-django-production
  IMAGE_NAME: django

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/798090991303/locations/global/workloadIdentityPools/github-pool/providers/github
          service_account: github-actions-deployer@artilence.iam.gserviceaccount.com

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Build & Push Docker Image (Daphne/ASGI)
        run: |
          docker build --platform linux/amd64 --target production \
            -t ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} .
          docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Deploy to Cloud Run (disable HTTP/2 for WebSockets)
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=4Gi \
            --cpu=2 \
            --timeout=3000 \
            --max-instances=3 \
            --min-instances=0 \
            --concurrency=100 \
            --no-use-http2 \
            --set-env-vars="DEBUG=False,DJANGO_SETTINGS_MODULE=management_app.config.settings" \
            --set-env-vars="BACKEND_API_BASE_URL=https://sooqsense-django-production-798090991303.asia-south2.run.app" \
            --set-env-vars="DB_HOST=ep-holy-hat-adj6g712-pooler.c-2.us-east-1.aws.neon.tech" \
            --set-env-vars="DB_PORT=5432" \
            --set-env-vars="DB_NAME=neondb" \
            --set-env-vars="DB_USER=neondb_owner" \
            --set-env-vars="DB_PASSWORD=npg_zy0JNK3kohmb" \
            --set-env-vars="OPENAI_API_KEY=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" \
            --set-env-vars="SERPER_API_KEY=da3cefd9584b4016a82e1e116b2badfcb07e5df1" \
            --set-env-vars="PINECONE_API_KEY=pcsk_EJHEg_B99xZ7vPcUkDSsD3Gj7KFwCeKgQCWKPvxX5894Cvy72z7DnXfdYCzN35pdp9M5K" \
            --set-env-vars="PINECONE_INDEX_NAME=artilence-production" \
            --set-env-vars="S3_BUCKET_NAME=sooqsense" \
            --set-env-vars="AWS_REGION=us-east-1" \
            --set-env-vars="AWS_ACCESS_KEY_ID=AKIAT7JJVEEMYAEX2UGD" \
            --set-env-vars="AWS_SECRET_ACCESS_KEY=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" \
            --set-env-vars="REDIS_URL=rediss://default:Ab6DAAIjcDE4YjIzMmJjYWY3NjE0NWIxYWIwNTg4MmNiMjkyNTc0Y3AxMA@ideal-calf-48771.upstash.io:6379" \
            --set-env-vars="CELERY_BROKER_URL=rediss://default:Ab6DAAIjcDE4YjIzMmJjYWY3NjE0NWIxYWIwNTg4MmNiMjkyNTc0Y3AxMA@ideal-calf-48771.upstash.io:6379" \
            --set-env-vars="CELERY_RESULT_BACKEND=rediss://default:Ab6DAAIjcDE4YjIzMmJjYWY3NjE0NWIxYWIwNTg4MmNiMjkyNTc0Y3AxMA@ideal-calf-48771.upstash.io:6379" \
            --set-env-vars="NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_Y2xpbWJpbmctcm9vc3Rlci02MC5jbGVyay5hY2NvdW50cy5kZXYk" \
            --set-env-vars="CLERK_SECRET_KEY=sk_test_VQ0vUgco3WhaI3iqA6okF3zCGV0XTAjpdnz0rJxgfi" \
            --set-env-vars="CLERK_BACKEND_AVAILABLE=https://api.clerk.com" \
            --set-env-vars="CLERK_FRONTEND_URL=https://climbing-rooster-60.clerk.accounts.dev" \
            --set-env-vars="CLERK_JWKS_URL=https://climbing-rooster-60.clerk.accounts.dev/.well-known/jwks.json" \
            --set-env-vars="GOOGLE_CLIENT_ID=229772932214-hil3ggbh30c83ag635c6ao6a4h91dvfo.apps.googleusercontent.com" \
            --set-env-vars="GOOGLE_CLIENT_SECRET=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" \
            --set-env-vars="GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback" \
            --set-env-vars="LINKEDIN_CLIENT_ID=77bbsr8z46dzgg" \
            --set-env-vars="LINKEDIN_CLIENT_SECRET=WPL_AP1.cPzBTYa3NNJXpstR.a2D98w==" \
            --set-env-vars="LINKEDIN_REDIRECT_URI=http://localhost:8000/auth/linkedin/callback" \
            --set-env-vars="LANGSMITH_TRACING=true" \
            --set-env-vars="LANGSMITH_API_KEY=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" \
            --set-env-vars="LANGSMITH_PROJECT=sooqsense-blog-generation" \
            --set-env-vars="LANGSMITH_ENDPOINT=https://api.smith.langchain.com" \
            --set-env-vars="LANGSMITH_WORKSPACE_ID=2979498f-fb22-57f7-a758-9cbbe3d40dca" \
            --set-env-vars="FAL_KEY=e59e15f3-ee4e-4847-9650-8e1bf514b772:c9aec65bceac24694cd7dff0ddba0533"

      - name: Get Service URL
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} --region=${{ env.REGION }} --format='value(status.url)')
          echo "API: $SERVICE_URL"
          echo "Docs: $SERVICE_URL/docs/"
          echo "Admin: $SERVICE_URL/admin/"

      - name: Health Check
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} --region=${{ env.REGION }} --format='value(status.url)')
          sleep 30
          curl -f "$SERVICE_URL/docs/" >/dev/null 2>&1 || echo "Docs not reachable yet"
